| original estimate | estimate | actual effort | remaining effort |
| ----------------- | -------- | ------------- | ---------------- |
| 4h                | -        | -             | -                |

**acceptance criteria:**
- we want to have a folder structure for a nodejs/bun-based project
- we want to have an npm package for a vite-based app
- we want npm packages for modules

**technical details:**
* root folders should be `guides/backlog` (tickets), `guides/knowledge` (topics), `sources/prospecto-app` (vite application), `sources/product-module` (npm workspace package)
